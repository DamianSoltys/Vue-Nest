{
  "name": "bsi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm i && (cd backend && npm i) && (cd ../frontend && npm i)",
    "start:dev": "concurrently \"cd backend && npm run start:dev\" \"cd frontend && npm run serve\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
